{
  "name": "sura-ui",
  "author": "asurraa",
  "version": "1.0.0",
  "description": "sura-ui root dir",
  "repository": {
    "type": "git",
    "url": "https://github.com/asurraa/sura-ui.git"
  },
  "homepage": "https://github.com/asurraa/sura-ui",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lerna": "lerna",
    "wizard": "npx lerna-wizard",
    "release-gen": "lerna version --conventional-commits --create-release github",
    "git": "sh scripts/git_script.sh",
    "build": "yarn lerna run build",
    "publish-example": "yarn && yarn lerna bootstrap && yarn build && cd example && yarn && yarn predeploy && yarn deploy"
  },
  "dependencies": {
    "antd": "^4.12.3",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-scripts": "4.0.1",
    "typescript": "^4.2.3"
  }
}
